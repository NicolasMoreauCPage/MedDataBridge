"""
Module de gestion des tâches asynchrones (stub).
"""
from fastapi import APIRouter

router = APIRouter(prefix="/tasks", tags=["Tasks"])

# TODO: Implémenter la gestion des tâches asynchrones
